//Diferencia entre Metodo y FUncion

const numero1 = 20;
const numero2 = "20";
                     

console.log(parseInt(numero2));    // parseInt() es una Funcion para pasar de string a numeros si o si tiene que ser un numero
console.log(numero2.toString());  // .toString() es un Metodo 

                                  // los que tienen un punto seguido del nombre de la funcion 
                                  // es por que son metodos, Los que tienen el nombre de 
                                  // la funcion seguido del parentesis es por que son  
                                  // Funciones.